{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the home page</h1>
                <span>Working with templating engines</span>
            </div>
        </div>
    </div>
{{end}}